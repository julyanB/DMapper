<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>DMapper</RootNamespace>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Title>DMapper</Title>
        <Authors>Julian B.</Authors>
        <Description>A lightweight .NET object mapping library for deep copying and attribute-based property mapping.</Description>
        <Copyright>Copyright © 2025 DMapper</Copyright>
        <PackageProjectUrl>https://github.com/julyan97/DMapper</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <RepositoryUrl>https://github.com/julyan97/DMapper</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageTags>object-mapper reflection mapping copy nested-objects</PackageTags>
        <PackageReleaseNotes>New Features &amp; Enhancements
Collection Mapping Support: Introduced the ability to map collections (List&lt;T&gt;, T[], and other IEnumerable&lt;T&gt; implementations) seamlessly using the MapTo&lt;T&gt; extension method.
Automatic Element Mapping: Each item in a source collection is individually mapped to the corresponding destination type, ensuring deep transformation support.
Flexible Destination Types: Supports mapping to arrays, generic lists, and collections with constructors accepting IEnumerable&lt;T&gt;.
Reflection-Based Mapping: Uses dynamic reflection to apply the appropriate MapTo&lt;T&gt; transformation to each element in the collection.</PackageReleaseNotes>
        <TargetFrameworks>net9.0;netstandard2.1</TargetFrameworks>
        <LangVersion>latest</LangVersion>
        <Version>2.1.0</Version>
        <PackageIcon>D-Logo.png</PackageIcon>
        <PackageId>DSeries.DMapper.Core</PackageId>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.CSharp" Version="4.7.0" />
        <PackageReference Include="CommunityToolkit.Diagnostics" Version="8.4.0" />
    </ItemGroup>

    <PropertyGroup>
        <PackageIcon>D-Logo.png</PackageIcon>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    </PropertyGroup>

    <ItemGroup>
        <None Include="D-Logo.png" Pack="true" PackagePath="." />
    </ItemGroup>
</Project>